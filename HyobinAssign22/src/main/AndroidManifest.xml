<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hyobin.im.s991526068">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyobinAssign2">
        <activity android:name=".ImActivity3" android:parentActivityName=".HyobinActivity"></activity>
        <activity android:name=".ImActivity2" android:parentActivityName=".HyobinActivity"/>
        <activity android:name=".ImActivity" android:parentActivityName=".HyobinActivity"/>
        <activity android:name=".HyobinActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>